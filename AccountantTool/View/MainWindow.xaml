<controls:MetroWindow
        x:Class="AccountantTool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:viewModel="clr-namespace:AccountantTool.ViewModel"
        BorderBrush="{DynamicResource AccentColorBrush}"

        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"

        xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"

        Title="Accountant tool"
        TitleAlignment="Center"

        IconTemplate="{DynamicResource IconTemplate}"
        ShowIconOnTitleBar="True"

        Width="1040"
        Height="570"
        MinWidth="1040"
        MinHeight="570"
        WindowStartupLocation="CenterScreen"

        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=viewModel:MainWindowViewModel}">
    <!--<Window.DataContext>
        <viewModel:MainWindowViewModel />
    </Window.DataContext>-->
    <Window.InputBindings>

    </Window.InputBindings>
    <Window.Resources>
        <DataTemplate x:Key="IconTemplate">
            <Button>
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Image Source="../Resources/Images/icon.png" />
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </DataTemplate>
    </Window.Resources>


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="292*" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>
        <DockPanel HorizontalAlignment="Stretch">
            <Menu Grid.Row="0">
                <MenuItem Header="{DynamicResource AT_recordsTitle}" Icon="{iconPacks:Modern ClipboardVariantEdit}">
                    <MenuItem
                        
                        Header="1"
                        Icon="{iconPacks:Material Autorenew}" />
                    <!--Command="{Binding LoadMedicamentRecordsCommand}"-->
                    <Separator Margin="-20,-6" />
                    <MenuItem
                        
                        Header="2"
                        Icon="{iconPacks:Entypo NewMessage}"
                         />
                    <!--IsEnabled="{Binding IsLoadedData}"
                    Command="{Binding AddNewMedicamentRecordCommand}"-->
                </MenuItem>
                <MenuItem Header="3" Icon="{iconPacks:Material Pill}" />
                <MenuItem Header="4" Icon="{iconPacks:Material Domain}" />
            </Menu>
            <StackPanel
                HorizontalAlignment="Right"
                Orientation="Horizontal"
                TextBlock.FontSize="14">
                <TextBlock
                    Height="Auto"
                    Margin="0,-1,0,0"
                    VerticalAlignment="Center"
                    Text="{DynamicResource AT_searchTitle}" />
                <TextBox
                    Width="200"
                    Height="20"
                    Margin="5,0"
                    VerticalContentAlignment="Center"/>
                    <!--mah:TextBoxHelper.Watermark="{Binding RandomWatermark, StringFormat={}Например: {0}, TargetNullValue=Загрузка..., Mode=TwoWay}"
                    IsEnabled="{Binding Path=IsLoadedData}"
                    Text="{Binding SearchString, UpdateSourceTrigger=PropertyChanged}" />-->
            </StackPanel>
        </DockPanel>
    </Grid>
</controls:MetroWindow>